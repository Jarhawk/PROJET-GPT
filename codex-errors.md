# Erreurs rencontrées lors du design Liquid Glass

## 20/06/2025
- `npm run dev` échoue : `vite` introuvable.
- `npm run lint` échoue : `@eslint/js` manquant.
- `npm test` échoue : `vitest` manquant.
